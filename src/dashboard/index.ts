// React-based modular dashboard - main export
export { initializeModularDashboard } from './reactIndex';

// Backwards compatibility (deprecated)
export { getDashboardInstance, addCustomModule } from './legacy';